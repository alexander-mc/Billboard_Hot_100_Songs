RSpec.describe BillboardHot100Songs do
  it "has a version number" do
    expect(BillboardHot100Songs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
